apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: orp-azure-prepare
  namespace: wac-hospital
spec:
  wait: true
  interval: 120s
  path: clusters/azure/prepare
  prune: true
  sourceRef:
    kind: GitRepository
    name: orp-azure-gitops-repo


apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: orp-azure-cd
  namespace: wac-hospital
spec:
  wait: true
  interval: 42s
  path: clusters/azure
  prune: true
  sourceRef:
    kind: GitRepository
    name: orp-azure-gitops-repo


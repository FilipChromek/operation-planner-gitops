apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: orp-or-planner-ufe
  namespace: wac-hospital
spec:
  imageRepositoryRef:
    name: orp-or-planner-ufe
  filterTags:
    pattern: "main.*"
  policy:
    alphabetical:
      order: asc

apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: orp-or-planner-webapi
  namespace: wac-hospital
spec:
  imageRepositoryRef:
    name: orp-or-planner-webapi
  filterTags:
    pattern: "main.*"
  policy:
    alphabetical:
      order: asc
